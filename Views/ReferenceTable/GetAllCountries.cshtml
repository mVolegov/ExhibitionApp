@model IEnumerable<ExhibitionApp.Models.Country>

@{
    ViewData["Title"] = "GetAllCountries";
}

<div class="text-center">
    <h1>Классификатор ОКСМ </h1>
    <h6 class="mb-3">Общероссийский классификатор стран мира</h6>
</div>

<div>
    <a class="btn btn-primary mb-3" asp-action="GetAllExtraTables"><b>&#8592</b> Назад к списку классификаторов</a>
</div>

<div class="card">
    <table class="table text-center table-striped table-borderless table-hover">
        <thead>
        <tr>
            <th>
                Код страны
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShortName)
            </th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ShortName)
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>